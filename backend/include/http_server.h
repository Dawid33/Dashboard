#ifndef BACKEND_HTTP_SERVER_H
#define BACKEND_HTTP_SERVER_H

/*
int
client_connection_handler (void *cls, struct MHD_Connection *connection,
                           const char *url,
                           const char *method, const char *version,
                           const char *upload_data,
                           size_t *upload_data_size, void **con_cls);
void deallocate_connection (void *cls, struct MHD_Connection *connection,
                        void **con_cls,
                        enum MHD_RequestTerminationCode toe);

*/
#endif //BACKEND_HTTP_SERVER_H
